import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.infectionblocks.game",
  appName: "Infection Blocks",
  webDir: "dist"
};

export default config;
